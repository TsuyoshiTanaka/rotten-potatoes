# kind: Cluster
# apiVersion: kind.x-k8s.io/v1alpha4

# nodes:
# - role: control-plane
# - role: control-plane
# - role: worker
# - role: worker
# - role: worker
#   extraPortMappings:
#   - containerPort: 30000
#     hostPort: 8080

# k3d cluster create --agents 3 -p "8080:30000@agent[0]" -p "8081:30001@agent[1]"
# k3d cluster delete 
